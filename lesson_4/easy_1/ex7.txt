What is the default return value of to_s when invoked on an object? Where could you go to find out if you want to be sure?

The default return value is the name of the object's class and an encoding of the object id. To find out for sure we can
check the documentation for that class.